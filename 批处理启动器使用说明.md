# 批处理启动器使用说明

## 📋 概述

本项目提供了两个批处理启动器，用于方便地启动投资分析系统：

1. **`start_personal_automation.bat`** - 个人投资自动化系统启动器
2. **`start_portfolio_scheduler.bat`** - 持股分析定时调度器启动器

## 🚀 使用方法

### 方法1：双击运行（推荐）
直接双击对应的 `.bat` 文件即可启动

### 方法2：命令行运行
在PowerShell中：
```powershell
.\start_personal_automation.bat
.\start_portfolio_scheduler.bat
```

在命令提示符中：
```cmd
start_personal_automation.bat
start_portfolio_scheduler.bat
```

## 📊 功能说明

### 🔹 个人投资自动化系统 (`start_personal_automation.bat`)

**功能**：
- 自动从数据库筛选500+只股票
- 进行多维度增强分析
- 生成个性化投资建议
- 自动发送邮件报告

**运行时间**：手动执行，约5-10分钟完成

**输出**：
- 筛选出15只高质量推荐股票
- 发送HTML格式的投资建议邮件
- 包含价格目标、风险评估、行业分析

### 🔹 持股分析定时调度器 (`start_portfolio_scheduler.bat`)

**功能**：
- 每日持股分析（交易日16:30）
- 每周深度分析（周日20:00）
- 每月组合优化（每月第一个周日20:00）
- 自动发送定时报告

**运行方式**：后台持续运行，按计划自动执行

**输出**：
- 持仓股票实时表现分析
- 盈亏统计和风险评估
- 技术指标和市场情绪分析
- 投资建议和组合优化建议

## ⚙️ 环境要求

### 📋 前置条件
1. **conda环境**：已安装并配置`openbb`环境
2. **Python依赖**：所有必要的Python包已安装
3. **数据库连接**：MySQL数据库可正常访问
4. **邮件配置**：邮件发送功能已正确配置

### 🔧 环境激活
批处理文件会自动执行以下步骤：
```batch
# 1. 激活conda环境
conda activate openbb

# 2. 检查环境激活状态
if %errorlevel% neq 0 (
    echo ❌ conda环境激活失败！
    pause
    exit /b 1
)

# 3. 运行Python脚本
python [对应的Python文件]
```

## 🛠️ 故障排除

### ❌ 常见错误及解决方案

**1. conda环境激活失败**
```
❌ conda环境激活失败！
请确保已安装conda并配置了openbb环境
```
**解决方案**：
- 确保已安装Anaconda或Miniconda
- 手动创建openbb环境：`conda create -n openbb python=3.10`
- 安装必要依赖：`pip install -r requirements.txt`

**2. TA-Lib报错**
```
WARNING - TA-Lib not found. Using fallback implementations
```
**解决方案**：
- 在openbb环境中安装TA-Lib：`conda install -c conda-forge ta-lib`
- 或使用pip：`pip install TA-Lib`

**3. 数据库连接失败**
```
❌ MySQL数据库连接失败
```
**解决方案**：
- 检查MySQL服务是否启动
- 验证数据库连接配置
- 确保网络连接正常

**4. 邮件发送失败**
```
❌ 邮件发送失败
```
**解决方案**：
- 检查邮件配置文件
- 验证SMTP服务器设置
- 确保邮箱授权码正确

## 📧 邮件接收

**默认邮箱**：`kidzss@gmail.com`

**邮件类型**：
- 📈 每日持股分析简报
- 📊 每周持股深度分析  
- 💰 每月持股组合优化分析
- 🎯 个人投资自动化推荐

## 🔄 定制配置

### 修改邮件接收地址
编辑配置文件：
- `portfolio_scheduler_config.json`
- `personal_investor_config.json`

### 调整运行时间
修改调度器配置：
```json
{
  "daily_time": "16:30",
  "weekly_time": "20:00", 
  "monthly_time": "20:00"
}
```

### 启用/禁用功能
```json
{
  "daily_enabled": true,
  "weekly_enabled": true,
  "monthly_enabled": true
}
```

## 📝 日志文件

**日志位置**：
- `portfolio_analysis_scheduler.log` - 调度器日志
- `personal_investor_automation.log` - 个人投资自动化日志

**日志级别**：INFO, WARNING, ERROR

## 🎯 最佳实践

1. **首次运行**：建议先手动运行一次，确保所有功能正常
2. **定期检查**：查看日志文件，监控系统运行状态
3. **数据更新**：确保市场数据及时更新
4. **备份配置**：定期备份重要的配置文件
5. **性能监控**：关注系统资源使用情况

## 🆘 技术支持

如遇到问题，请检查：
1. 日志文件中的错误信息
2. conda环境是否正确激活
3. 所有依赖包是否已安装
4. 数据库和网络连接是否正常

---

*最后更新：2025-06-17*
*版本：v0.02* 